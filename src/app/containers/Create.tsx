import { Typography } from "@material-ui/core";

function Create() {
  return (
    <div>
      <Typography
        color="textSecondary"
        variant="h6"
        component="h2"
        gutterBottom
      >
        Create a New Note
      </Typography>
    </div>
  );
}

export default Create;
